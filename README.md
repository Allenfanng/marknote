# MarkNote

类 Typora 的所见即所得 Markdown 桌面编辑器。

![MarkNote](src-tauri/icons/128x128.png)

**当前版本：v1.7.0** · Windows 10/11 (x64) · Tauri v2 + React 19 + Milkdown

![MarkNote 界面截图](docs/screenshot.png)

## 功能特性

### 编辑

- **所见即所得** — Milkdown Crepe（ProseMirror）驱动的富文本 Markdown 编辑
- **源码模式** — `Ctrl+/` 一键切换，全页编辑原始 Markdown
- **多标签页** — 浏览器风格标签管理文档；重复打开已打开的文件自动定位到对应标签
- **缩放** — `Ctrl+滚轮` 全局缩放并持久化
- **YAML frontmatter** — 开头元数据解析为等宽面板，不会渲染成巨大标题
- **自动保存** — 定时自动保存 + 关闭未保存文档时确认提示

### 图表

- **Mermaid / SVG 渲染** — ` ```mermaid ` 与 ` ```svg ` 代码块就地渲染成图，光标移入编辑源码、移出恢复图形
- **插入图表** — 工具栏一键插入 Mermaid / SVG 模板，改改就能用
- **全屏查看** — 图太小看不清？悬浮「放大」进全屏：滚轮/按钮缩放、拖拽平移、`0` 适应窗口、`1` 原始大小、`Esc` 关闭
- **导出 SVG** — 一键把图存成独立 `.svg` 文件（固定像素尺寸，外部工具直接可用）

### 表格

- 插入对话框自定义行列数
- 鼠标悬停表格边缘即可**加行 / 加列**，左上角手柄菜单支持**删行 / 删列 / 删表**
- 表头行受保护，避免误删破坏表格结构

### 文件

- 打开 / 保存 `.md` `.markdown` `.txt`，安装后自动关联文件类型
- 从资源管理器拖入文件直接打开
- 单实例运行：再次打开文件会在已有窗口中新开标签
- 侧边栏快速浏览与打开文件
- **导出 HTML / PDF**，图表会渲染为内联图片

### 外观

- 日间 / 夜间主题，Notion/Linear 风格中性配色
- **显示模式切换**：窄栏（固定 860px 居中，专注阅读）与宽栏（正文跟随窗口铺满）
- 中文字体优化（标题使用霞鹜文楷）

## 下载安装

从 [GitHub Releases](https://github.com/Allenfanng/marknote/releases) 下载：

| 文件 | 说明 |
|---|---|
| `MarkNote_x.y.z_x64-setup.exe` | **推荐**，标准安装版，自动处理 WebView2 依赖 |
| `MarkNote_x.y.z_x64_en-US.msi` | MSI 安装包，适合批量 / 静默部署 |
| `marknote_x.y.z_portable.exe` | 便携版，免安装（需系统已装 WebView2） |

## 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+N` | 新建标签页 |
| `Ctrl+O` | 打开文件 |
| `Ctrl+S` | 保存 |
| `Ctrl+/` | 切换源码模式 |
| `Ctrl+滚轮` | 缩放编辑区 |

## 技术栈

| 层 | 技术 |
|---|---|
| 桌面壳 | Tauri v2 |
| 后端 | Rust（Tauri 命令 + rfd 文件对话框） |
| 前端 | React 19 + TypeScript |
| 编辑器核心 | Milkdown Crepe (ProseMirror) |
| 图表 | Mermaid（按需懒加载，不影响启动速度） |
| 图标 | lucide-react |
| 构建 | Vite 8 |

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run tauri:dev

# 构建安装包（msi + nsis + portable，产物自动同步到 release/ 目录）
npm run tauri:build
```

需要 Rust 工具链与 Node.js。首次构建会自动下载 WiX / NSIS 打包工具。

## 版本历史

### v1.7.0（2026-09-05）

- 工具栏新增「插入图表」下拉：Mermaid 图 / SVG 图一键插入模板
- 图表预览新增悬浮操作条：全屏查看 / 导出 SVG / 编辑源码
- 图表全屏查看器：缩放（按钮 / 滚轮 / `+` `-`）、拖拽平移、`0` 适应窗口、`1` 原始大小
- 导出 SVG 为固定像素尺寸并带 xml 声明，外部查看器可直接打开
- 解决编辑器内宽图（时序图等）被压缩后文字看不清的问题

### v1.6.0（2026-09-04）

- 表格边缘控件：悬停表格右缘 / 下缘浮出「+」加行加列，左上角手柄菜单删行 / 删列 / 删表（含表头行保护）
- Mermaid / SVG 图表渲染：```mermaid 与 ```svg 代码块就地出图，点击图表编辑源码
- 显示模式切换：窄栏（固定 860px）/ 宽栏（跟随窗口），偏好持久化
- mermaid 独立 chunk 按需加载，不影响冷启动
- 导出 HTML / PDF 时图表渲染为内联图片

### v1.5.0（2026-08-30）

- 打开已打开的文件时切换到对应标签，不再重复创建
- 关闭全部标签后显示空状态页面
- 工具栏、状态栏、标签栏交互与样式增强
- 编辑器初始化稳定性修复

### v1.4.2（2026-08-15）

- 修复 frontmatter 插件 options 缺省导致的编辑器初始化崩溃
- 预览/导出中表格与代码块修复
- MarkText 风格细节微调

### v1.4.1（2026-08-15）

- YAML frontmatter 解析为等宽面板
- 修复开头元数据被渲染成巨大标题

### v1.4.0（2026-08-15）

- Typora 风格排版重构
- 缩放全局生效并持久化
- 标题过大修复

### v1.3.0（2026-07-24）

- 导出修复
- `Ctrl+滚轮` 缩放
- 标签右键菜单
- 支持 `.txt` 文件
- 启动与脏标记修复

<details>
<summary>v1.2.x 及更早版本</summary>

### v1.2.5（2026-07-24）

- 窗口焦点恢复
- 标签栏滚轮切换
- 版本号自动化

### v1.2.4（2026-06-07）

- 单实例文件打开
- 构建发布优化
- 源码编辑修复与 UI 改进

### v1.2.3（2026-06-02）

- 文件关联修复
- PDF 导出修复
- 启动时间显示
- 关于对话框新增联系邮箱

### v1.2.2（2026-06-02）

- 编辑器懒加载与即时内容预览，大幅优化启动速度
- 文件关联修复
- 调试代码清理

### v1.2.1（2026-06-02）

- 浏览器风格标签页
- 导出修复
- 加载状态提示

### v1.1.0（2026-06-01）

- 修复主题切换（编辑区跟随亮/暗模式）
- 新增顶部工具栏（Lucide 图标 + 激活态高亮）
- 新增源码模式（`Ctrl+/` 切换）
- 新增文件拖拽（新窗口打开）
- 新增关于对话框
- 表格单元格边框优化
- 中文字体优化（标题使用霞鹜文楷/微软雅黑 UI）
- UI 重构（Notion/Linear 风格）
- 新应用图标

### v1.0.0

- WYSIWYG Markdown 编辑
- 文件打开/保存
- 亮色/暗色主题
- 键盘快捷键
- 未保存修改提示
- Windows 安装包

</details>

## 作者

FZ
