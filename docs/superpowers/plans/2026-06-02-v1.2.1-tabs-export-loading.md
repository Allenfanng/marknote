# v1.2.1 改进实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 添加浏览器式标签页、修复导出按钮、添加启动加载状态

**Architecture:** 状态驱动标签页系统，App.tsx 维护 Tab[] 数组替代单文件状态；切换标签时保存当前编辑器内容并重建编辑器；TabBar 组件替代 TitleBar

**Tech Stack:** React 19, TypeScript, Milkdown Crepe, Tauri v2, lucide-react

---

## 文件变更清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 创建 | `src/components/TabBar.tsx` | 标签栏组件（标签列表 + 新建 + 操作按钮） |
| 删除 | `src/components/TitleBar.tsx` | 被 TabBar 替代 |
| 修改 | `src/App.tsx` | 核心重构：tabs 状态管理、标签操作、文件操作适配 |
| 修改 | `src/App.css` | TabBar 样式、加载动画、导出菜单修复 |
| 修改 | `src/components/Toolbar.tsx` | 版本号更新 |
| 修改 | `src-tauri/tauri.conf.json` | 版本号更新 |
| 修改 | `CLAUDE.md` | 版本日志 |

---

### Task 1: 修复导出菜单 CSS 溢出

**Files:**
- Modify: `src/App.css:147`

- [ ] **Step 1: 修改 toolbar overflow**

将 `.toolbar` 的 `overflow-x: auto` 改为 `overflow: visible`。按钮均有 `flex-shrink: 0` 不会溢出，滚动条本无必要，而 `overflow-x: auto` 裁剪了绝对定位的导出下拉菜单。

```css
.toolbar {
  display: flex;
  align-items: center;
  height: 40px;
  padding: 0 8px;
  background: var(--toolbar-bg);
  border-bottom: 1px solid var(--border-color);
  flex-shrink: 0;
  gap: 2px;
  overflow: visible;
}
```

- [ ] **Step 2: 验证**

运行 `npm run tauri:dev`，点击工具栏右侧导出按钮，确认下拉菜单正常出现。

---

### Task 2: 添加编辑器加载状态

**Files:**
- Modify: `src/App.css`
- Modify: `src/App.tsx`

- [ ] **Step 1: 添加加载动画 CSS**

在 `src/App.css` 末尾（Milkdown 覆盖之前）添加：

```css
/* --- Editor loading --- */

.editor-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: var(--text-secondary);
  gap: 12px;
  font-size: 13px;
}

.spinner {
  width: 24px;
  height: 24px;
  border: 2px solid var(--border-color);
  border-top-color: var(--accent-color);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```

- [ ] **Step 2: 修改 App.tsx 编辑区域渲染**

在 App.tsx 的 `return` 中，将编辑区域的渲染改为：

```tsx
<div className="editor-wrapper">
  {viewMode === 'wysiwyg' ? (
    <main className="editor-container">
      {!editorReady && (
        <div className="editor-loading">
          <div className="spinner" />
          <span>Loading...</span>
        </div>
      )}
      <Editor
        key={editorKey}
        defaultValue={content}
        onChange={handleEditorChange}
        editorRef={editorRef}
      />
    </main>
  ) : (
    <SourceView value={sourceContent} onChange={handleSourceChange} />
  )}
  <StatusBar content={currentContent} />
</div>
```

注意：Milkdown 初始化期间 Editor 组件会渲染空 DOM，加载动画叠在上面；编辑器就绪后 Milkdown 的内容自然覆盖。如果担心闪烁，可以在 `editorReady` 后才显示 Editor，但会导致编辑器重建时有空白期。当前方案更平滑。

- [ ] **Step 3: 验证**

运行 `npm run tauri:dev`，新开窗口时观察编辑区域：应先显示 spinner，然后切换到编辑器内容。

---

### Task 3: 创建 TabBar 组件

**Files:**
- Create: `src/components/TabBar.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: 创建 TabBar.tsx**

```tsx
import { Moon, Sun, Code2, Plus, X } from 'lucide-react'

export interface Tab {
  id: string
  filePath: string | null
  content: string
  sourceContent: string
  isDirty: boolean
  viewMode: 'wysiwyg' | 'source'
}

interface TabBarProps {
  tabs: Tab[]
  activeTabId: string
  onTabClick: (id: string) => void
  onTabClose: (id: string) => void
  onNewTab: () => void
  theme: 'light' | 'dark'
  onToggleTheme: () => void
  viewMode: 'wysiwyg' | 'source'
  onToggleViewMode: () => void
}

export default function TabBar({
  tabs,
  activeTabId,
  onTabClick,
  onTabClose,
  onNewTab,
  theme,
  onToggleTheme,
  viewMode,
  onToggleViewMode,
}: TabBarProps) {
  return (
    <div className="tab-bar">
      <div className="tab-bar-tabs">
        {tabs.map((tab) => {
          const fileName = tab.filePath
            ? tab.filePath.split(/[\\/]/).pop()
            : 'Untitled'
          const isActive = tab.id === activeTabId
          return (
            <div
              key={tab.id}
              className={`tab-item ${isActive ? 'active' : ''}`}
              onClick={() => onTabClick(tab.id)}
            >
              <span className="tab-name">{fileName}</span>
              {tab.isDirty && <span className="tab-dirty-dot" />}
              <button
                className="tab-close"
                onClick={(e) => {
                  e.stopPropagation()
                  onTabClose(tab.id)
                }}
                title="Close"
              >
                <X size={12} />
              </button>
            </div>
          )
        })}
        <button className="tab-new" onClick={onNewTab} title="New tab (Ctrl+N)">
          <Plus size={14} />
        </button>
      </div>
      <div className="tab-bar-actions">
        <button
          className={`titlebar-btn ${viewMode === 'source' ? 'active' : ''}`}
          onClick={onToggleViewMode}
          title="Source code (Ctrl+/)"
        >
          <Code2 size={16} />
        </button>
        <button
          className="titlebar-btn"
          onClick={onToggleTheme}
          title="Toggle theme"
        >
          {theme === 'light' ? <Moon size={16} /> : <Sun size={16} />}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 添加 TabBar 样式**

在 `src/App.css` 中，替换原来的 `/* --- TitleBar --- */` 区域（第 74-134 行）为：

```css
/* --- TabBar --- */

.tab-bar {
  display: flex;
  align-items: flex-end;
  height: 38px;
  background: var(--titlebar-bg);
  border-bottom: 1px solid var(--border-color);
  -webkit-app-region: drag;
  user-select: none;
  padding: 0 4px 0 8px;
  flex-shrink: 0;
}

.tab-bar-tabs {
  display: flex;
  align-items: flex-end;
  gap: 0;
  flex: 1;
  overflow-x: auto;
  -webkit-app-region: no-drag;
}

.tab-bar-tabs::-webkit-scrollbar {
  height: 0;
}

.tab-item {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  padding: 0 10px 0 12px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-bottom: none;
  border-radius: 6px 6px 0 0;
  color: var(--text-secondary);
  font-size: 12px;
  cursor: pointer;
  max-width: 180px;
  min-width: 80px;
  flex-shrink: 0;
  transition: background 0.15s, color 0.15s;
}

.tab-item:hover {
  background: var(--bg-hover);
}

.tab-item.active {
  background: var(--bg-primary);
  color: var(--text-primary);
  border-bottom: 1px solid var(--bg-primary);
  margin-bottom: -1px;
  padding-bottom: 1px;
}

.tab-name {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tab-dirty-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent-color);
  flex-shrink: 0;
}

.tab-close {
  display: none;
  align-items: center;
  justify-content: center;
  width: 18px;
  height: 18px;
  border: none;
  background: none;
  color: var(--text-secondary);
  border-radius: 3px;
  cursor: pointer;
  flex-shrink: 0;
  padding: 0;
}

.tab-item:hover .tab-close {
  display: flex;
}

.tab-close:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
}

.tab-new {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  background: none;
  border: none;
  color: var(--text-secondary);
  border-radius: 4px;
  cursor: pointer;
  font-size: 18px;
  flex-shrink: 0;
  margin-bottom: 2px;
}

.tab-new:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
}

.tab-bar-actions {
  display: flex;
  gap: 2px;
  -webkit-app-region: no-drag;
  margin-bottom: 2px;
}
```

保留原有的 `.titlebar-btn` 样式不变（TabBar 的操作按钮复用此样式）。

- [ ] **Step 3: 验证**

运行 `npx tsc -b` 确认 TabBar.tsx 无类型错误。

---

### Task 4: 重构 App.tsx 为标签页状态模型

**Files:**
- Modify: `src/App.tsx`

这是最大的改动。核心变更：将单文件状态替换为 Tab 数组，所有操作通过 activeTab 派生。

- [ ] **Step 1: 替换导入和顶层工具函数**

将 App.tsx 顶部改为：

```tsx
import { useState, useCallback, useEffect, useRef } from 'react'
import { invoke } from '@tauri-apps/api/core'
import { listen } from '@tauri-apps/api/event'
import { getCurrentWebview } from '@tauri-apps/api/webview'
import Editor, { type EditorHandle } from './components/Editor'
import TabBar, { type Tab } from './components/TabBar'
import Toolbar from './components/Toolbar'
import SourceView from './components/SourceView'
import Sidebar from './components/Sidebar'
import StatusBar from './components/StatusBar'
import './App.css'
```

移除 `WebviewWindow` 导入。移除 `openFileInNewWindow` 函数。

添加工具函数：

```tsx
const MD_EXTENSIONS = ['.md', '.markdown', '.txt']
const AUTO_SAVE_INTERVAL = 3 * 60 * 1000

let tabIdCounter = 0
function nextTabId() { return `tab-${++tabIdCounter}` }

function isMarkdownFile(path: string): boolean {
  const lower = path.toLowerCase()
  return MD_EXTENSIONS.some((ext) => lower.endsWith(ext))
}

function createTab(overrides?: Partial<Tab>): Tab {
  const content = '# Welcome to MarkNote\n\nStart typing...\n'
  return {
    id: nextTabId(),
    filePath: null,
    content,
    sourceContent: content,
    isDirty: false,
    viewMode: 'wysiwyg',
    ...overrides,
  }
}
```

- [ ] **Step 2: 替换组件状态**

将 App 函数内的状态声明替换为：

```tsx
function App() {
  const [tabs, setTabs] = useState<Tab[]>([createTab()])
  const [activeTabId, setActiveTabId] = useState(tabs[0].id)
  const [theme, setTheme] = useState<'light' | 'dark'>('light')
  const editorRef = useRef<EditorHandle | null>(null)
  const [editorReady, setEditorReady] = useState(false)
  const [isDragging, setIsDragging] = useState(false)
  const [editorKey, setEditorKey] = useState(0)
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const [closeConfirm, setCloseConfirm] = useState<{ tabId: string; tabName: string } | null>(null)

  // Refs for latest values
  const tabsRef = useRef(tabs)
  const activeTabIdRef = useRef(activeTabId)

  useEffect(() => { tabsRef.current = tabs }, [tabs])
  useEffect(() => { activeTabIdRef.current = activeTabId }, [activeTabId])

  const activeTab = tabs.find((t) => t.id === activeTabId) ?? tabs[0]

  const updateTab = useCallback((tabId: string, updates: Partial<Tab>) => {
    setTabs((prev) =>
      prev.map((t) => (t.id === tabId ? { ...t, ...updates } : t))
    )
  }, [])
```

移除旧的：`filePath, content, isDirty, sourceContent, viewMode` 状态及其 refs。只保留 `editorRef, editorReady, isDragging, editorKey, sidebarOpen`。

- [ ] **Step 3: 实现 saveCurrentEditorContent 辅助函数**

在状态声明之后添加：

```tsx
  const saveCurrentEditorContent = useCallback(() => {
    if (!editorRef.current?.ready) return
    const md = editorRef.current.getMarkdown()
    updateTab(activeTabIdRef.current, { content: md })
  }, [updateTab])
```

- [ ] **Step 4: 重写 loadFile**

```tsx
  const loadFile = useCallback(async (path: string) => {
    try {
      const text = await invoke<string>('read_file', { path })
      const newTab = createTab({
        filePath: path,
        content: text,
        sourceContent: text,
      })
      setTabs((prev) => [...prev, newTab])
      setActiveTabId(newTab.id)
      setEditorReady(false)
      setEditorKey((k) => k + 1)
      invoke('add_recent_file', { path }).catch(console.error)
    } catch (e) {
      console.error('Failed to load file:', e)
    }
  }, [])
```

- [ ] **Step 5: 重写 handleExportHtml 和 handleExportPdf**

导出函数从旧 ref 模式改为 activeTab 模式。`markdownToHtml` 和 `getExportCss` 工具函数保持在文件底部不变。

```tsx
  const handleExportHtml = useCallback(async () => {
    try {
      const tab = tabsRef.current.find((t) => t.id === activeTabIdRef.current)
      if (!tab) return
      const md = tab.viewMode === 'wysiwyg' && editorRef.current?.ready
        ? editorRef.current.getMarkdown()
        : (tab.viewMode === 'source' ? tab.sourceContent : tab.content)
      const html = markdownToHtml(md)
      const css = getExportCss()
      await invoke('export_html', { markdown: html, css })
    } catch (e) {
      console.error('Export HTML failed:', e)
    }
  }, [])

  const handleExportPdf = useCallback(() => {
    const tab = tabsRef.current.find((t) => t.id === activeTabIdRef.current)
    if (!tab) return
    const md = tab.viewMode === 'wysiwyg' && editorRef.current?.ready
      ? editorRef.current.getMarkdown()
      : (tab.viewMode === 'source' ? tab.sourceContent : tab.content)
    const html = markdownToHtml(md)
    const css = getExportCss()
    const fullHtml = `<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>MarkNote Export</title>
<style>${css}</style>
</head>
<body><div class="markdown-body">${html}</div></body>
</html>`
    const blob = new Blob([fullHtml], { type: 'text/html' })
    const url = URL.createObjectURL(blob)
    const win = window.open(url, '_blank')
    if (win) {
      win.onload = () => { win.print() }
    }
  }, [])
```

- [ ] **Step 7: 重写 handleOpen（检查已有标签）**

```tsx
  const handleOpen = useCallback(async () => {
    try {
      const path = await invoke<string | null>('open_file_dialog')
      if (!path) return
      const existingTab = tabsRef.current.find((t) => t.filePath === path)
      if (existingTab) {
        if (existingTab.id !== activeTabIdRef.current) {
          saveCurrentEditorContent()
          setActiveTabId(existingTab.id)
          setEditorReady(false)
          setEditorKey((k) => k + 1)
        }
      } else {
        await loadFile(path)
      }
    } catch (e) {
      console.error('Open failed:', e)
    }
  }, [loadFile, saveCurrentEditorContent])
```

- [ ] **Step 8: 重写 handleSave**

```tsx
  const handleSave = useCallback(async () => {
    try {
      const tab = tabsRef.current.find((t) => t.id === activeTabIdRef.current)
      if (!tab) return

      let content: string
      if (tab.viewMode === 'wysiwyg' && editorRef.current?.ready) {
        content = editorRef.current.getMarkdown()
      } else {
        content = tab.viewMode === 'source' ? tab.sourceContent : tab.content
      }

      let path = tab.filePath
      if (!path) {
        path = await invoke<string | null>('save_file_dialog')
        if (!path) return
      }

      await invoke('write_file', { path, content })
      updateTab(tab.id, { filePath: path, content, isDirty: false })
    } catch (e) {
      console.error('Save failed:', e)
    }
  }, [updateTab])
```

- [ ] **Step 9: 重写 handleNewTab、handleTabClick、handleTabClose**

```tsx
  const handleNewTab = useCallback(() => {
    saveCurrentEditorContent()
    const newTab = createTab({
      content: '# Untitled\n\n',
      sourceContent: '# Untitled\n\n',
    })
    setTabs((prev) => [...prev, newTab])
    setActiveTabId(newTab.id)
    setEditorReady(false)
    setEditorKey((k) => k + 1)
  }, [saveCurrentEditorContent])

  const handleTabClick = useCallback((tabId: string) => {
    if (tabId === activeTabIdRef.current) return
    saveCurrentEditorContent()
    setActiveTabId(tabId)
    setEditorReady(false)
    setEditorKey((k) => k + 1)
  }, [saveCurrentEditorContent])

  const closeTab = useCallback((tabId: string) => {
    setTabs((prev) => {
      const idx = prev.findIndex((t) => t.id === tabId)
      const newTabs = prev.filter((t) => t.id !== tabId)

      if (newTabs.length === 0) {
        const newTab = createTab({
          content: '# Untitled\n\n',
          sourceContent: '# Untitled\n\n',
        })
        setActiveTabId(newTab.id)
        setEditorReady(false)
        setEditorKey((k) => k + 1)
        return [newTab]
      }

      if (tabId === activeTabIdRef.current) {
        const newActiveIdx = Math.min(idx, newTabs.length - 1)
        setActiveTabId(newTabs[newActiveIdx].id)
        setEditorReady(false)
        setEditorKey((k) => k + 1)
      }

      return newTabs
    })
  }, [])

  const handleTabClose = useCallback((tabId: string) => {
    const tab = tabsRef.current.find((t) => t.id === tabId)
    if (!tab) return
    if (tab.isDirty) {
      const fileName = tab.filePath ? tab.filePath.split(/[\\/]/).pop()! : 'Untitled'
      setCloseConfirm({ tabId, tabName: fileName })
      return
    }
    closeTab(tabId)
  }, [closeTab])
```

- [ ] **Step 10: 添加关闭确认对话框处理函数**

```tsx
  const handleCloseConfirmSave = useCallback(async () => {
    const { tabId } = closeConfirm!
    setCloseConfirm(null)

    const tab = tabsRef.current.find((t) => t.id === tabId)
    if (!tab) return

    let path = tab.filePath
    if (!path) {
      try {
        path = await invoke<string | null>('save_file_dialog')
        if (!path) return
      } catch {
        return
      }
    }

    try {
      const content = tab.viewMode === 'source' ? tab.sourceContent : tab.content
      await invoke('write_file', { path, content })
      closeTab(tabId)
    } catch (e) {
      console.error('Save failed:', e)
    }
  }, [closeConfirm, closeTab])

  const handleCloseConfirmDiscard = useCallback(() => {
    const { tabId } = closeConfirm!
    setCloseConfirm(null)
    closeTab(tabId)
  }, [closeConfirm, closeTab])

  const handleCloseConfirmCancel = useCallback(() => {
    setCloseConfirm(null)
  }, [])
```

- [ ] **Step 11: 重写 handleEditorChange 和 handleSourceChange**

```tsx
  const handleEditorChange = useCallback((markdown: string) => {
    updateTab(activeTabIdRef.current, { content: markdown, isDirty: true })
  }, [updateTab])

  const handleSourceChange = useCallback((value: string) => {
    updateTab(activeTabIdRef.current, { sourceContent: value, isDirty: true })
  }, [updateTab])
```

- [ ] **Step 12: 重写 toggleTheme 和 toggleViewMode**

```tsx
  const toggleTheme = useCallback(() => {
    setTheme((prev) => (prev === 'light' ? 'dark' : 'light'))
  }, [])

  const toggleViewMode = useCallback(() => {
    const tab = tabsRef.current.find((t) => t.id === activeTabIdRef.current)
    if (!tab) return
    if (tab.viewMode === 'wysiwyg') {
      const md = editorRef.current?.getMarkdown() ?? tab.content
      updateTab(tab.id, { viewMode: 'source', sourceContent: md })
    } else {
      updateTab(tab.id, { viewMode: 'wysiwyg' })
      setEditorReady(false)
      setEditorKey((k) => k + 1)
    }
  }, [updateTab])
```

- [ ] **Step 13: 重写自动保存 effect**

```tsx
  useEffect(() => {
    const id = setInterval(() => {
      const tab = tabsRef.current.find((t) => t.id === activeTabIdRef.current)
      if (tab?.isDirty && tab.filePath) {
        handleSave()
      }
    }, AUTO_SAVE_INTERVAL)
    return () => clearInterval(id)
  }, [handleSave])
```

- [ ] **Step 14: 修改拖拽事件处理**

```tsx
  useEffect(() => {
    const webview = getCurrentWebview()
    const unlistenPromise = webview.onDragDropEvent((event) => {
      if (event.payload.type === 'enter') {
        setIsDragging(true)
      } else if (event.payload.type === 'drop') {
        setIsDragging(false)
        const mdFile = event.payload.paths.find(isMarkdownFile)
        if (mdFile) {
          const existingTab = tabsRef.current.find((t) => t.filePath === mdFile)
          if (existingTab) {
            if (existingTab.id !== activeTabIdRef.current) {
              saveCurrentEditorContent()
              setActiveTabId(existingTab.id)
              setEditorReady(false)
              setEditorKey((k) => k + 1)
            }
          } else {
            loadFile(mdFile)
          }
        }
      } else if (event.payload.type === 'leave') {
        setIsDragging(false)
      }
    })
    return () => {
      unlistenPromise.then((fn) => fn())
    }
  }, [loadFile, saveCurrentEditorContent])
```

- [ ] **Step 15: 修改侧边栏文件打开处理**

```tsx
  const handleSidebarFileOpen = useCallback((path: string) => {
    const existingTab = tabsRef.current.find((t) => t.filePath === path)
    if (existingTab) {
      if (existingTab.id !== activeTabIdRef.current) {
        saveCurrentEditorContent()
        setActiveTabId(existingTab.id)
        setEditorReady(false)
        setEditorKey((k) => k + 1)
      }
    } else {
      loadFile(path)
    }
  }, [loadFile, saveCurrentEditorContent])
```

- [ ] **Step 16: 修改快捷键处理**

```tsx
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      const mod = e.ctrlKey || e.metaKey
      if (mod && e.key === 's') {
        e.preventDefault()
        handleSave()
      } else if (mod && e.key === 'o') {
        e.preventDefault()
        handleOpen()
      } else if (mod && e.key === 'n') {
        e.preventDefault()
        handleNewTab()
      } else if (mod && e.key === '/') {
        e.preventDefault()
        toggleViewMode()
      }
    }
    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [handleSave, handleOpen, handleNewTab, toggleViewMode])
```

- [ ] **Step 17: 重写 return JSX**

```tsx
  const currentContent = activeTab.viewMode === 'source' ? activeTab.sourceContent : activeTab.content

  return (
    <div className="app">
      <TabBar
        tabs={tabs}
        activeTabId={activeTabId}
        onTabClick={handleTabClick}
        onTabClose={handleTabClose}
        onNewTab={handleNewTab}
        theme={theme}
        onToggleTheme={toggleTheme}
        viewMode={activeTab.viewMode}
        onToggleViewMode={toggleViewMode}
      />
      <Toolbar
        editor={editorRef.current}
        disabled={activeTab.viewMode === 'source' || !editorReady}
        onNew={handleNewTab}
        onOpen={handleOpen}
        onSave={handleSave}
        sidebarOpen={sidebarOpen}
        onToggleSidebar={() => setSidebarOpen((v) => !v)}
        onExportHtml={handleExportHtml}
        onExportPdf={handleExportPdf}
      />
      <div className="main-area">
        {sidebarOpen && (
          <Sidebar
            filePath={activeTab.filePath}
            theme={theme}
            onFileOpen={handleSidebarFileOpen}
          />
        )}
        <div className="editor-wrapper">
          {activeTab.viewMode === 'wysiwyg' ? (
            <main className="editor-container">
              {!editorReady && (
                <div className="editor-loading">
                  <div className="spinner" />
                  <span>Loading...</span>
                </div>
              )}
              <Editor
                key={editorKey}
                defaultValue={activeTab.content}
                onChange={handleEditorChange}
                editorRef={editorRef}
              />
            </main>
          ) : (
            <SourceView value={activeTab.sourceContent} onChange={handleSourceChange} />
          )}
          <StatusBar content={currentContent} />
        </div>
      </div>
      {isDragging && (
        <div className="drop-overlay">
          <div className="drop-overlay-text">Drop to open in new tab</div>
        </div>
      )}
      {closeConfirm && (
        <dialog
          open
          className="close-confirm-dialog"
          onClick={(e) => { if (e.target === e.currentTarget) handleCloseConfirmCancel() }}
        >
          <div className="close-confirm-content">
            <p>Save changes to "{closeConfirm.tabName}"?</p>
            <div className="close-confirm-actions">
              <button className="close-confirm-btn primary" onClick={handleCloseConfirmSave}>Save</button>
              <button className="close-confirm-btn" onClick={handleCloseConfirmDiscard}>Don't Save</button>
              <button className="close-confirm-btn" onClick={handleCloseConfirmCancel}>Cancel</button>
            </div>
          </div>
        </dialog>
      )}
    </div>
  )
```

- [ ] **Step 18: 添加关闭确认对话框 CSS**

在 `src/App.css` 末尾添加：

```css
/* --- Close confirm dialog --- */

.close-confirm-dialog {
  border: none;
  border-radius: 10px;
  padding: 0;
  background: var(--bg-primary);
  color: var(--text-primary);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  max-width: 360px;
  width: 90vw;
}

.close-confirm-dialog::backdrop {
  background: rgba(0, 0, 0, 0.4);
}

.close-confirm-content {
  padding: 24px;
}

.close-confirm-content p {
  margin: 0 0 20px;
  font-size: 14px;
}

.close-confirm-actions {
  display: flex;
  gap: 8px;
  justify-content: flex-end;
}

.close-confirm-btn {
  padding: 6px 16px;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 13px;
  cursor: pointer;
  transition: background 0.12s;
}

.close-confirm-btn:hover {
  background: var(--bg-hover);
}

.close-confirm-btn.primary {
  background: var(--accent-color);
  color: #fff;
  border-color: var(--accent-color);
}

.close-confirm-btn.primary:hover {
  opacity: 0.9;
}
```

- [ ] **Step 19: 移除 handleNew 函数**

删除旧的 `handleNew` 函数（已被 `handleNewTab` 替代）。

- [ ] **Step 20: 验证**

运行 `npx tsc -b` 确认无类型错误。运行 `npm run tauri:dev` 测试：
- 启动后应显示标签栏（一个 "Welcome to MarkNote" 标签）
- Ctrl+N 新建标签
- Ctrl+O 打开文件到新标签
- 点击标签切换
- 关闭标签（干净的和有修改的）
- 拖拽文件到窗口打开新标签
- 导出按钮下拉菜单正常
- 启动时有加载动画

---

### Task 5: 删除 TitleBar.tsx

**Files:**
- Delete: `src/components/TitleBar.tsx`

- [ ] **Step 1: 删除文件**

删除 `src/components/TitleBar.tsx`。App.tsx 中已不再引用它。

- [ ] **Step 2: 验证**

运行 `npx tsc -b` 确认编译通过。

---

### Task 6: 更新版本号

**Files:**
- Modify: `src/components/Toolbar.tsx`
- Modify: `src-tauri/tauri.conf.json`
- Modify: `CLAUDE.md`

- [ ] **Step 1: 更新 tauri.conf.json 版本**

将 `"version": "1.2.0"` 改为 `"version": "1.2.1"`。

- [ ] **Step 2: 更新关于对话框版本**

在 `src/components/Toolbar.tsx` 中，将：
```tsx
<p className="about-version">v1.2.0</p>
<p className="about-date">2026-06-02</p>
```
改为：
```tsx
<p className="about-version">v1.2.1</p>
<p className="about-date">2026-06-02</p>
```

- [ ] **Step 3: 更新 CLAUDE.md**

在 CLAUDE.md 的版本概述中，将 `v1.2.0` 改为 `v1.2.1`。

在 CLAUDE.md 末尾（`## 待改进` 之前）添加：

```markdown
## v1.2.1 更新内容（2026-06-02）

### 新增

- 浏览器式标签页（替换标题栏，支持新建/切换/关闭/未保存确认）
- 编辑器加载动画（启动和切换标签时显示 spinner）

### 修复

- 导出下拉菜单被 toolbar overflow 裁剪导致不可见
- 拖拽文件改为在当前窗口新开标签页（不再开新窗口）

### 改进

- Ctrl+N/Ctrl+O/侧边栏点击均打开新标签页
- 侧边栏点击已打开文件时切换到对应标签
- 关闭最后一个标签页自动新建 Untitled 标签
```

将 `待改进` 中的 `- [ ] 多标签页` 改为 `- [x] 多标签页`。

- [ ] **Step 4: 验证**

运行 `npm run tauri:dev` 全面测试所有功能。
